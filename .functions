#!/bin/bash
## lazy git function
function lazygit() {
   if [ $# != 0 ]
	then
    	
    	git add .
        git commit -m "$*"
    	git push
    else
    	echo " Ponle nombre gilipollas"
    fi

}

## function to open .bashrc easily
function bababui() {
    gedit ~/.bashrc
}

function xex() {
    for folder in ./*
    do
    	$(git -C $folder/ rev-parse 2>/dev/null)
    	if [ $? == 0 ]
	then
	cd $(realpath $folder)
	    if [[ `git status --porcelain` ]]; then
	        git add .
                git commit -m "Security_commit $folder $(date +%H:%M:%S)" 1>/dev/null
    	        git push 2>/dev/null
    	        echo "Actualizando $folder"
	    else
  		echo "Sin cambios een $folder"
	    fi
	cd ~/
	    
    	    
	fi
    done
    
    
}
